<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace:名称空间
id：唯一标识
resultType:返回值类型
#{id}:从传递过来的参数取出id值-->
<mapper namespace="com.ad.mapper.PostMapper">

    <select id="getPostById" resultType="com.ad.pojo.PostInfo">
      select * from post_info where post_id = #{id}
    </select>

    <select id="getPostByTitle" resultType="com.ad.pojo.PostInfo">
      select * from post_info where post_title like '%' #{title} '%'
    </select>

    <select id="getPostByContent" resultType="com.ad.pojo.PostInfo">
      select * from post_info where post_content like '%' #{content} '%'
    </select>

    <select id="getPostByTag" resultType="com.ad.pojo.PostInfo">
      select * from post_info where post_id in (select post_id from tag_link where tag_id in (select tag_id from tag_info where tag_content like '%' #{content} '%'))
    </select>


    <insert id="addPost" parameterType="com.ad.pojo.PostInfo" useGeneratedKeys="true" keyProperty="postId">
      insert into post_info(post_title,post_content,user_id,create_time,update_time)
      values (#{postTitle},#{postContent},#{userId},#{createTime},#{updateTime})
    </insert>

    <update id="updatePost" parameterType="com.ad.pojo.PostInfo">
     update post_info set post_title=#{postTitle},post_content=#{postContent},comment_num=#{commentNum},
     user_id=#{userId},create_time=#{createTime},update_time=#{updateTime}
     where post_id = #{postId}
    </update>

    <delete id="deletePostById" >
    delete from post_info where post_id=#{id}
    </delete>

    <select id="getPageOrderByTime" resultType="com.ad.pojo.PostInfo">
      select * from post_info  order by update_time desc,post_id desc limit ${(offset-1)*size},${size}
    </select>

    <select id="getPageOrderByTimeAndGrade" resultType="com.ad.pojo.PostInfo">
      select * from post_info where post_id in (select post_id from tag_link where tag_id in (select tag_id from tag_info where tag_content like '%' #{content} '%'))  order by update_time desc,post_id desc limit ${(offset-1)*size},${size}
    </select>

    <select id="getPageOrderByAuthor" resultType="com.ad.pojo.PostInfo">
      select * from post_info where user_id  = #{userId} order by update_time desc,post_id desc limit ${(offset-1)*size},${size}
    </select>

    <select id="getPostNum"  resultType="Integer">
         select count(*) from  post_info
    </select>

    <select id="getMyPostNum" parameterType="Integer" resultType="Integer">
         select count(*) from post_info where user_id = #{userId}
    </select>

</mapper>
